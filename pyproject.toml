[project]
name = "serverless-transcribe"
version = "0.1.0"
description = "Add description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "modal>=1.5.0",
    "requests>=2.34.2",
]
